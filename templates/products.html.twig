{% extends 'layout.html.twig' %}

{% block content %}
	<h2>Tous nos produits</h2>

	<div class="products-list">
		{% for bike in bikes %}
			<a href="product/{{ bike.id }}">
				{% if bike.image %}
					<img src="/images/{{ bike.image }}"/>
				{% endif %}
                <h2>{{ bike.name }}</h2>
				£{{ bike.price }}
			</a>
		{% endfor %}
	</div>

{% endblock %}

